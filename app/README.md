# App placeholder

This directory is reserved for the Next.js application.

At this stage the repository only provides the target location and a placeholder Docker image so `docker compose build` and `docker compose up` can include the frontend container in the overall system layout.

When frontend implementation begins, initialize the Next.js project in this directory and replace the placeholder container workflow with the real application build and start commands.
